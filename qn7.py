'''isinstance method: isinstamce method is used to check whether an object belongs to a
#specified class or any of its sub class'''

a = "diwascodes"
b = 20
print(isinstance(a, str))
print(isinstance(b, int))
